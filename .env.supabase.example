# Supabase Configuration
# Get these values from your Supabase project dashboard:
# 1. Go to https://app.supabase.com
# 2. Select your project
# 3. Go to Settings > API
# 4. Copy the Project URL and anon/public key

# Your Supabase project URL
VITE_SUPABASE_URL=https://your-project-ref.supabase.co

# Your Supabase anon/public key (safe to use in client-side code)
VITE_SUPABASE_ANON_KEY=your-anon-public-key-here

# ============================================================================
# INSTRUCTIONS:
# ============================================================================
# 1. Copy this file and rename it to `.env` (without the .example extension)
# 2. Replace the placeholder values with your actual Supabase credentials
# 3. Never commit the `.env` file to version control (it's in .gitignore)
# 4. Restart your dev server after creating/updating .env: npm run dev
# ============================================================================

# ============================================================================
# SECURITY NOTES:
# ============================================================================
# - The ANON key is safe to use in client-side code (it's public)
# - Row Level Security (RLS) protects your data even with the public key
# - Never share your Supabase service_role key (not needed for this app)
# - Never commit .env to git (already in .gitignore)
# ============================================================================
