name: (disabled) Frontend deploy via Vercel

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Frontend deploy is handled by Vercel. This workflow is intentionally disabled."
